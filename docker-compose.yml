version: '3'
services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data


volumes:
  pgdata:
